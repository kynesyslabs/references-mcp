# MCP Server Configuration
MCP_PORT=3000

# Environment
NODE_ENV=production

# Optional: Custom base path for documentation
# DOCS_PATH=/path/to/demosdk-api-ref

# Optional: Custom host binding (default: all interfaces)
# HOST=127.0.0.1